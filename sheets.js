const { google } = require('googleapis');

async function getSheetData(sheetId, range) {
    // Placeholder function to fetch data from Google Sheets
    return [];
}

module.exports = { getSheetData };
